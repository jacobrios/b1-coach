# Which session data the debriefs in this directory were written about.
#
# Read by scripts/grade-coach-accuracy.mjs. A --builder flag that disagrees
# with the line below is refused outright rather than obeyed, because grading
# a round through the wrong builder does not fail: it produces a complete,
# plausible-looking fact sheet for the wrong fifteen swings, and every verdict
# computed from it is garbage that reads like a result.
#
# These 64 debriefs were generated on 19 August 2026 at commit 8b07ab0,
# BEFORE Slice 9 replaced session 1's fifteen hand-written swings. The
# working tree no longer holds what they describe, so they must be graded
# against the frozen snapshot in ../session-one-before.mjs, which is what the
# slice9-before builder reads.
#
# Do not edit this file to make a command work. It was written by whoever
# generated the round, which is the only moment the answer was known.

#
# The seed matters as much as the builder, and for a reason that is easy to
# miss: session 1 is the same fifteen swings at any seed, but sessions 2, 3
# and 4 are GENERATED from it with a seeded PRNG, so the seed alone decides
# what those sessions contain. Grading this round at the wrong seed rebuilds
# sessions that never existed for every multi-session cell while the
# session-1 cells stay correct, and the report that comes out looks partly
# sane rather than obviously broken.

# ANNOTATION, 20 AUGUST 2026, SLICE 11 TASK 1. THE LINE BELOW IS UNCHANGED,
# AND SAYING WHY IS THE POINT OF THIS PARAGRAPH.
#
# The three sibling rounds beside and above this one had their builder line
# repointed today, from "current" to "slice11-before". This one did not, and
# a reader comparing the files deserves to know that is deliberate rather
# than an omission.
#
# "slice9-before" was REPOINTED, NOT RENAMED. Its meaning was always "what
# this round was written against", and that meaning has now been completed
# rather than changed. It used to mean the frozen fifteen session-1 swings in
# ../session-one-before.mjs plus whatever generator the working tree happened
# to hold. It now means those same fifteen swings plus the generator frozen
# at commit 53315e5, snapshotted at
# docs/eval-fixtures/frozen/swing-generator-pre-slice11.mjs.
#
# Why that matters here: the generator was the second moving part nobody had
# pinned. Sessions 2, 3 and 4 are generated from session 1, so this round's
# 40 multi-session records depended on src/swingGenerator.js just as much as
# on the fifteen swings, and Slice 11 rewrites it. Freezing the snapshot is
# what keeps this round gradeable; the name on this line did not need to
# change for that to be true.

builder = slice9-before
handed-era = current
seed = 20260814
